<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xmlns:dubbo="http://code.alibabatech.com/schema/dubbo"
       xsi:schemaLocation="http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans.xsd http://code.alibabatech.com/schema/dubbo http://code.alibabatech.com/schema/dubbo/dubbo.xsd">

    <!-- 注册中心注册配置 -->
    <dubbo:application name="${rpc.dubbo.application.name}" />
    <dubbo:registry address="${rpc.dubbo.registry.address}" />
    <dubbo:protocol name="${rpc.dubbo.protocol.name}" port="${rpc.dubbo.protocol.port}" />

    <!-- 服务对应的BEAN -->
    <bean id="helloService" class="com.yc256.intra.service.impl.HelloWorldServiceImpl" />

    <!-- dubbo提供的服务 -->
    <dubbo:service interface="com.yc256.intra.service.HelloWorldService" ref="helloService"/>

    <!-- 将多个配置文件读取到容器中，交给Spring管理 -->
    <bean id="myPropertyConfigurer" class="org.springframework.beans.factory.config.PropertyPlaceholderConfigurer">
        <property name="locations">
            <list>
                <value>classpath*:config/dev/zookeeper.properties</value>
            </list>
        </property>
    </bean>
</beans>